import 'package:flutter/material.dart';

class ShoppingAdminView extends StatelessWidget {
  const ShoppingAdminView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('shopping view for admin'),
    );
  }
}